#include <iostream>


#include  "json.cpp"


int main() {




 /*   std::cout << "-----------------------------test operatori di stream___________________________________________________"
              << "\n";

        json j; // dichiara un json
        json z;
        std::cout << "Inserisci il testo del json 1: "; // chiede all'utente di inserire il testo del json
        std::cin >> j; // legge il json dallo standard inpu usando il metodo definito
    std::cout << "Inserisci il testo del json 2: "; // chiede all'utente di inserire il testo del json
    std::cin>>z;// stampa un messaggio
    std::cout << "Il json letto 1 e: " << std::endl;
    std::cout << j << std::endl; // stampa il json
    std::cout << "Il json letto 2 e: " << std::endl;
    std::cout << z << std::endl; // stampa il json
        z.push_back(j);
        z.push_front(z);
    std::cout << "Il json finale e : " << std::endl;
    std::cout<<z;
*/

    std::cout << "-----------------------------test2___________________________________________________";

    json k;
    std::cout << "Inserisci il testo del json 1: ";
    std::cin >> k;
    std::cout<<k;




        return 0;

}